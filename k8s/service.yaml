apiVersion: v1
kind: Service
metadata:
  name: flask-stock-service
spec:
  selector:
    app: flask-stock-app
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
  type: NodePort